language: python
python:
  - "3.5" # current version
services:
  - mysql
env:
  global:
    - DJANGO_SETTINGS_MODULE="project.settings.travis_settings"
  matrix:
    - DB=mysql
before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS travis_ci_db DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;'
  - python -m pip install --upgrade pip
install:
  - pip install -r requirements.txt
script:
  - python manage.py test
